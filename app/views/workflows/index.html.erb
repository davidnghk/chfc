<p id="notice"><%= notice %></p>

<h1>Workflows</h1>

<table>
  <thead>
    <tr>
      <th>Work</th>
      <th>Location</th>
      <th>Incharge user</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @workflows.each do |workflow| %>
      <tr>
        <td><%= workflow.work_id %></td>
        <td><%= workflow.location %></td>
        <td><%= workflow.incharge_user_id %></td>
        <td><%= link_to 'Show', workflow %></td>
        <td><%= link_to 'Edit', edit_workflow_path(workflow) %></td>
        <td><%= link_to 'Destroy', workflow, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Workflow', new_workflow_path %>
